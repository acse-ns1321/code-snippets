// -------------- VECTOR ----------------------------
	for(auto d : v1)
		cout <<  d;

// -------------- SET --------------------------------
	for (auto d : s1)
		cout << "\t" << d;

// -------------- UNORDERED SET -----------------------
	for (auto d : us1)
		cout << "\t" << d;

// -------------- MAP ---------------------------------
	for (auto d : m1)
		cout << "\t" << d.first << "-" << d.second;

// -------------- MULTI MAP -----------------------
	for (auto d : mm1)
		cout << "\t" << d.first << "-" << d.second;